# Техническое задание (ТЗ)
## Проект: Чат-бот «Михалыч» в мессенджере MAX

- Версия: 1.0.0
- Дата: 2026-02-16
- Статус: Draft
- Язык интерфейса: русский

---

## 1. Цель и область работ

### 1.1. Цель
Реализовать корпоративного чат-бота «MAX-версия Михалыча» в мессенджере MAX для:
- авторизации пользователя;
- предоставления главного меню с сервисными разделами;
- поиска сотрудников;
- просмотра корпоративных ресурсов с пагинацией;
- отображения информации по отпуску.

### 1.2. Область работ (MVP)
В MVP включаются:
- сценарий старта и авторизации через кнопку «Начать»;
- главное меню с кнопками;
- разделы:
  - «Поиск сотрудников»;
  - «Ресурсы компании» (страничный вывод);
  - «Хочу в отпуск»;
- глобальная навигация «🏠 Главное меню»;
- обработка невалидного ввода и базовые ошибки.

Разделы «Помощь СИБ», «Загрузить тренировку», «Пин-понг» остаются в статусе pause (временно недоступны в MVP).

---

## 2. Термины и сущности

- **Пользователь** — сотрудник, взаимодействующий с ботом в MAX.
- **Авторизован** — пользователь успешно прошел первичную проверку/привязку.
- **Состояние диалога** — текущий контекст пользователя (главное меню, ожидание фамилии и т.д.).
- **Ресурс компании** — ссылка на внутренний или внешний корпоративный сервис.

---

## 3. Пользовательские сценарии

## 3.1. Сценарий старта `/start`

1. Пользователь отправляет `/start`.
2. Бот отвечает:

   ```
   👋 Привет, Я — MAX-версия Михалыча. Корпоративный чат-бот компании Онланта.

   🔐 Для начала работы мне нужно вас авторизовать.

   ✅ Нажмите кнопку «Начать», чтобы продолжить.
   ```

3. Бот отображает кнопку `Начать`.
4. После нажатия `Начать`:
   - при успехе авторизации бот показывает главное меню;
   - при ошибке — сообщение об ошибке и повторно кнопку `Начать`.

## 3.2. Главное меню

После успешной авторизации бот показывает кнопки:

1. `Поиск сотрудников`
2. `Ресурсы компании`
3. `Помощь СИБ` *(pause)*
4. `Хочу в отпуск`
5. `Загрузить тренировку` *(pause)*
6. `Пин-понг` *(pause)*
7. `Показать пароль WI-FI`
8. `Руководителю`
9. `Экспресс-отчет по контрагенту`

### Правило навигации
В любом разделе, кроме самого главного меню, бот обязан показывать кнопку `🏠 Главное меню`.
При нажатии `🏠 Главное меню` бот возвращает пользователя в главное меню и сбрасывает контекст раздела.

## 3.3. Раздел «Поиск сотрудников»

1. Пользователь нажимает `Поиск сотрудников`.
2. Бот отвечает: `Введите фамилию сотрудника`.
3. Бот показывает кнопку `🏠 Главное меню`.
4. Пользователь вводит фамилию.

### Валидация фамилии (обязательная)
- входное значение проходит `trim` (удаление пробелов по краям);
- длина после `trim`: от 2 до 50 символов;
- допустимые символы: буквы, пробел, дефис;
- минимум 2 буквенных символа в строке.

### Обработка результата поиска
- если найден 1 сотрудник: вывести карточку сотрудника;
- если найдено несколько: вывести список (до 10) и предложить уточнить запрос;
- если не найдено: сообщение `Сотрудник не найден. Проверьте написание фамилии.`;
- если сервис поиска недоступен: сообщение об ошибке и предложение повторить позже.

## 3.4. Раздел «Ресурсы компании»

1. Пользователь нажимает `Ресурсы компании`.
2. Бот отправляет заголовок:
   `Ресурсы компании (Страница X/N)`.
3. Бот отображает:
   - до 5 кнопок-ссылок на ресурсы;
   - `⬅️ Предыдущая` (если `X > 1`);
   - `Следующая ➡️` (если `X < N`);
   - `🏠 Главное меню`.

### Логика пагинации
- размер страницы: 5 ресурсов;
- `N = ceil(кол-во активных ресурсов / 5)`;
- при нажатии `Следующая ➡️` показать страницу `X + 1`;
- при нажатии `⬅️ Предыдущая` показать страницу `X - 1`.

Если активных ресурсов нет, бот отправляет:
`Список ресурсов временно пуст.` и показывает `🏠 Главное меню`.

## 3.5. Раздел «Хочу в отпуск»

1. Пользователь нажимает `Хочу в отпуск`.
2. Бот отправляет сообщение:

`Всего у тебя 28 календарных дней отпуска в году (если ты их не отгуляешь, они переносятся на следующий год). Одна часть твоего отпуска должна составлять 14 календарных дней единовременно. Чтобы пойти в отпуск тебе нужно: 1. Предварительно согласовать даты отпуска с руководителем, чтобы не было пересечений с другими коллегами. 2. Завести заявку на Интранете в разделе "Отпуска и отсутствия". Не позднее, чем за 6 рабочих дней ты должен получить согласование руководителя. 3. Жди отпускные не позднее, чем за 3 дня до отпуска.`

3. Бот показывает кнопку `🏠 Главное меню`.

## 3.6. Разделы в статусе pause

Для кнопок:
- `Помощь СИБ`
- `Загрузить тренировку`
- `Пин-понг`

Бот в MVP должен отвечать:
`Раздел временно недоступен.` и показывать `🏠 Главное меню`.

## 3.7. Разделы без детализированной логики (MVP-заглушка)

Для кнопок:
- `Показать пароль WI-FI`
- `Руководителю`
- `Экспресс-отчет по контрагенту`

До утверждения детальной бизнес-логики бот отвечает:
`Функция в разработке.` и показывает `🏠 Главное меню`.

---

## 4. Функциональные требования (FR)

### 4.1. Авторизация
- **FR-AUTH-001**: Бот должен обрабатывать команду `/start`.
- **FR-AUTH-002**: При первом входе бот должен отправлять приветственный текст и кнопку `Начать`.
- **FR-AUTH-003**: Нажатие `Начать` инициирует процедуру авторизации пользователя.
- **FR-AUTH-004**: После успешной авторизации бот переводит пользователя в состояние `AUTHORIZED` и показывает главное меню.
- **FR-AUTH-005**: При ошибке авторизации бот показывает понятную ошибку и кнопку `Начать` для повтора.

### 4.2. Главное меню и навигация
- **FR-MENU-001**: Бот должен отображать 9 кнопок главного меню в фиксированном порядке.
- **FR-MENU-002**: Кнопка `🏠 Главное меню` обязательна во всех разделах, кроме главного меню.
- **FR-MENU-003**: Нажатие `🏠 Главное меню` всегда возвращает пользователя в главное меню и очищает состояние раздела.

### 4.3. Поиск сотрудников
- **FR-EMP-001**: При входе в раздел бот должен запросить фамилию.
- **FR-EMP-002**: Бот валидирует фамилию по правилам длины и символов.
- **FR-EMP-003**: Для невалидного ввода бот должен отправлять причину и пример корректного ввода.
- **FR-EMP-004**: Бот должен выполнять поиск по валидному запросу.
- **FR-EMP-005**: Бот корректно обрабатывает 0/1/много результатов.

### 4.4. Ресурсы компании
- **FR-RES-001**: Бот выводит ресурсы страницами по 5 элементов.
- **FR-RES-002**: Бот должен отображать номер текущей страницы и общее число страниц.
- **FR-RES-003**: Кнопки пагинации показываются только при допустимом переходе.
- **FR-RES-004**: Каждая кнопка ресурса открывает URL, заданный в конфигурации.

### 4.5. Хочу в отпуск
- **FR-VAC-001**: Раздел выводит утвержденный текст инструкции по отпуску.
- **FR-VAC-002**: После сообщения бот показывает кнопку `🏠 Главное меню`.

### 4.6. Обработка неизвестного ввода
- **FR-COM-001**: В неизвестном контексте бот отправляет `Я не понял команду. Используйте кнопки меню.`.
- **FR-COM-002**: После неизвестного ввода бот предлагает `🏠 Главное меню`.

---

## 5. Нефункциональные требования (NFR)

- **NFR-001 (Производительность)**:
  - ответ на локальные команды/кнопки: до 2 сек (p95);
  - ответ с внешними интеграциями: до 5 сек (p95).
- **NFR-002 (Доступность)**: доступность сервиса не ниже 99.5% в месяц.
- **NFR-003 (Локализация)**: все пользовательские сообщения на русском языке.
- **NFR-004 (Надежность)**: при временной недоступности внешнего сервиса бот не падает, а возвращает контролируемую ошибку.
- **NFR-005 (Наблюдаемость)**: логирование запросов, ошибок и ключевых событий сценариев.

---

## 6. Требования к безопасности

- **SEC-001**: доступ к персональным данным только для авторизованных пользователей.
- **SEC-002**: не логировать чувствительные данные в открытом виде (PII, токены, пароли).
- **SEC-003**: проверять входные данные и отклонять потенциально опасные payload.
- **SEC-004**: доступ к интеграциям должен выполняться через сервисные учетные данные/токены.

---

## 7. Логирование и аналитика

Логировать минимум следующие события:
- `start_received`
- `auth_started`
- `auth_success`
- `auth_failed`
- `menu_opened`
- `employee_search_requested`
- `employee_search_validation_failed`
- `employee_search_done`
- `resources_opened`
- `resources_page_changed`
- `vacation_info_opened`
- `feature_paused_opened`
- `unknown_input`

Для каждого события сохранять:
- timestamp;
- user_id (внутренний идентификатор);
- scenario/state;
- результат (success/error);
- код ошибки (если есть).

---

## 8. Состояния бота (state machine)

Минимальный набор состояний:
- `NEW`
- `WAITING_START_CONFIRM`
- `AUTHORIZED`
- `IN_EMPLOYEE_SEARCH`
- `IN_RESOURCES`
- `IN_VACATION_INFO`

Переходы:
- `/start` -> `WAITING_START_CONFIRM`
- `Начать` + успех -> `AUTHORIZED`
- `Поиск сотрудников` -> `IN_EMPLOYEE_SEARCH`
- `Ресурсы компании` -> `IN_RESOURCES`
- `Хочу в отпуск` -> `IN_VACATION_INFO`
- `🏠 Главное меню` -> `AUTHORIZED`

---

## 9. Ошибки и fallback-сценарии

- Ошибка авторизации: `Не удалось авторизовать. Попробуйте еще раз.` + `Начать`.
- Таймаут внешнего сервиса: `Сервис временно недоступен. Повторите позже.` + `🏠 Главное меню`.
- Некорректная фамилия: пояснение правил + повторный ввод + `🏠 Главное меню`.
- Неизвестная команда: `Я не понял команду. Используйте кнопки меню.` + `🏠 Главное меню`.

---

## 10. Критерии приемки (UAT)

1. При `/start` пользователь всегда получает приветствие и кнопку `Начать`.
2. После успешной авторизации показывается полное главное меню из 9 кнопок.
3. Кнопка `🏠 Главное меню` доступна во всех разделах и возвращает в меню.
4. Поиск сотрудников отклоняет невалидные фамилии и принимает валидные.
5. Ресурсы компании выводятся по 5 элементов на страницу, пагинация корректна.
6. Раздел «Хочу в отпуск» показывает утвержденный текст без искажений.
7. Pause-разделы возвращают `Раздел временно недоступен.`.
8. При неизвестном вводе бот корректно обрабатывает сценарий fallback.

---

## 11. Ограничения и допущения

- Детальная логика разделов `Показать пароль WI-FI`, `Руководителю`, `Экспресс-отчет по контрагенту` будет уточнена отдельно.
- Интеграция поиска сотрудников зависит от корпоративного каталога.
- Список ресурсов компании передается из конфигурации или внешнего источника.

---

## 12. Артефакты

- Машиночитаемая версия требований: `max_bot_requirements.json`.
